# @deniffer/sitemap-watch-cli

Competitor sitemap snapshot CLI for product growth and SEO workflows.

```bash
bunx @deniffer/sitemap-watch-cli --schema
bunx @deniffer/sitemap-watch-cli registry dataset competitors --registry-file ./registry.json
```

Registry files are local inputs. Keep private competitor lists out of public repositories if needed.
